using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class PlayerProperties
{
    private static int currHealth = 100; // Player's health
    private static int maxHealth = 100; // Player's maximum health
}
